{
  "name": "pi-wsl-mcp",
  "version": "0.1.0",
  "description": "An MCP bridge for the Pi coding agent running in WSL.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "pi-wsl-mcp": "src/cli.mjs"
  },
  "files": [
    "src",
    "scripts",
    "run-pi-wsl-mcp.cmd",
    "README.md"
  ],
  "engines": {
    "node": ">=22.19.0"
  },
  "scripts": {
    "start": "node src/cli.mjs",
    "check": "node --check src/cli.mjs && node --check src/pi-rpc.mjs && node --check src/pi-service.mjs && node --check src/server.mjs && node --check src/util.mjs && node --check src/eol-extension.mjs && node --test",
    "test": "node --test",
    "smoke:mcp": "node scripts/mcp-smoke.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "^2.0.0",
    "zod": "^4.2.0"
  },
  "devDependencies": {
    "@modelcontextprotocol/client": "^2.0.0"
  }
}
